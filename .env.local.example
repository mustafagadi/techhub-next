# عنوان الـ API كما يراه المتصفح (مسار نسبي يُوجَّه عبر next.config.js)
NEXT_PUBLIC_API_BASE=/api

# عنوان الـ API كما يراه خادم Next (للـ server components) — عنوان كامل
API_BASE_SERVER=http://localhost:5080/api

# معرّف Google Analytics (اتركه فارغًا لتعطيل التحليلات)
NEXT_PUBLIC_GA_ID=
